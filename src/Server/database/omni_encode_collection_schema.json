{
  "$jsonSchema": {
    "bsonType": "object",
    "required": [
      "name",
      "id",
      "department",
      "encode"
    ],
    "properties": {
      "_id": {
        "bsonType": "string",
        "description": "must be a string and is unique"
      },
      "id": {
        "bsonType": "string",
        "description": "must be a string and is required"
      },
      "name": {
        "bsonType": "string",
        "description": "must be a string and is required"
      },
      "department": {
        "bsonType": "string",
        "description": "must be a string and is required"
      },
      "encode": {
        "bsonType": "array",
        "description": "must be an array of number"
      }
    }
  }
}